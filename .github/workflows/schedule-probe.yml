name: Schedule Probe

on:
  schedule:
    # Scheduled by repository owner to refresh GitHub Actions schedule actor.
    - cron: "*/5 * * * *"

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Print schedule probe time
        run: |
          echo "Schedule probe fired"
          date -u